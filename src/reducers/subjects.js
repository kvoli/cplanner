import { subjectList } from "../assets/subjectList";

const initialState = subjectList

const subjectsObject = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default subjectsObject;